export default function PokemonList({ pokemons }) {
  return (
    <div>
      {/* {
        pokemons.map((pokemon, i) =>
        <div key={pokemon + i}>
              pokemon.pokemon
            <div/>
        )
      } */}
    </div>
  );
}